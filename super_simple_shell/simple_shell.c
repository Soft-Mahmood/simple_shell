#include "shell.h"

/**
 * this is an interactive shell
 * Runs commands with their full path.
 * commands must have no arguments.
 *
 */
int main(int argc, char **argv, char **env)
{
	return (EXIT_SUCCESS);
}
